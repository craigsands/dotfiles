#!/usr/bin/env bash
# Claude PostToolUse hook — formats .md files with mdformat.

file_path=$(jq -r '.tool_input.file_path // empty')

[[ -z "$file_path" ]] && exit 0
[[ "$file_path" != *.md ]] && exit 0

mdformat "$file_path"
